public class Main {
    public static void main(String[] args) {
        int n = args.length / 2;
        int[] merged = new int[args.length];
        for (int i = 0; i < args.length; i++) merged[i] = Integer.parseInt(args[i]);
        for (int num : merged) System.out.print(num + " ");
    }
}
