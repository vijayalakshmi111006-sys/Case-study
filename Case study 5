public class Main {
    public static void main(String[] args) {
        String code = args[0];
        String rev = new StringBuilder(code).reverse().toString();
        System.out.println(code.equals(rev) ? "Valid" : "Invalid");
    }
}
