import java.util.Arrays;

public class Main {
    public static void main(String[] args) {
        int n = args.length / 2;
        int[] arr1 = new int[n];
        int[] arr2 = new int[n];
        for (int i = 0; i < n; i++) {
            arr1[i] = Integer.parseInt(args[i]);
            arr2[i] = Integer.parseInt(args[i + n]);
        }
        System.out.println(Arrays.equals(arr1, arr2) ? "Identical" : "Not identical");
    }
}
